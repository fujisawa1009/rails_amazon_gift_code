<h1>CSVインポート</h1>

<%= form_with(model: @csv_import, local: true) do |form| %>
  <div class="field">
    <%= form.label :file, 'CSVファイル' %>
    <%= form.file_field :file, accept: '.csv' %>
  </div>

  <div class="actions">
    <%= form.submit 'インポート開始' %>
  </div>
<% end %>
